<Page
    x:Class="WinSteroid.App.Views.ApplicationPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:converters="using:WinSteroid.App.Converters"
    xmlns:models="using:WinSteroid.Common.Models"
    xmlns:viewModels="using:WinSteroid.App.ViewModels"
    mc:Ignorable="d"
    Background="{ThemeResource ApplicationPageBackgroundThemeBrush}"
    DataContext="{Binding Application, Source={StaticResource Locator}}">

    <Grid>
        <Grid.ChildrenTransitions>
            <TransitionCollection>
                <EntranceThemeTransition />
            </TransitionCollection>
        </Grid.ChildrenTransitions>
        
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <Grid Grid.Row="0"
              Height="48"
              Background="{ThemeResource SystemControlBackgroundChromeMediumBrush}">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto" />
                <ColumnDefinition Width="*" />
            </Grid.ColumnDefinitions>

            <Button Grid.Column="0"
                    Content="{x:Bind ViewModel.SelectedIcon.Icon, Converter={StaticResource ApplicationIconToGlyphConverter}}"
                    Command="{x:Bind ViewModel.BackCommand, Mode=OneWay}"
                    Padding="16,12"
                    Height="48"
                    Width="48"
                    VerticalAlignment="Stretch"
                    Foreground="{ThemeResource SystemControlForegroundAccentBrush}"
                    Style="{ThemeResource NavigationBackButtonNormalStyle}" />

            <TextBlock Grid.Column="1" 
                       Text="{x:Bind ViewModel.Name, Mode=OneWay}"
                       VerticalAlignment="Center"
                       Style="{ThemeResource BaseTextBlockStyle}" />
        </Grid>

        <StackPanel Grid.Row="1"
                    Padding="12">
            <TextBlock Text="Preferences" 
                       Style="{ThemeResource SubtitleTextBlockStyle}" />

            <ComboBox HorizontalAlignment="Stretch" 
                      Header="Icon"
                      Margin="0,24,0,0"
                      ItemsSource="{x:Bind ViewModel.AvailableIcons, Mode=OneWay}"
                      SelectedItem="{x:Bind ViewModel.SelectedIcon, Mode=TwoWay}">
                <ComboBox.ItemTemplate>
                    <DataTemplate x:DataType="viewModels:IconViewModel">
                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="Auto" />
                                <ColumnDefinition Width="*" />
                            </Grid.ColumnDefinitions>

                            <TextBlock Grid.Column="0" 
                                       FontFamily="Segoe MDL2 Assets"
                                       VerticalAlignment="Center"
                                       Text="{x:Bind Icon, Converter={StaticResource ApplicationIconToGlyphConverter}}" />

                            <TextBlock Grid.Column="1" 
                                       TextAlignment="Left" 
                                       Margin="8,0"
                                       Text="{x:Bind Name}" />
                        </Grid>
                    </DataTemplate>
                </ComboBox.ItemTemplate>
            </ComboBox>

            <ToggleSwitch Header="Muted" 
                          IsOn="{x:Bind ViewModel.Muted, Mode=TwoWay}"
                          Margin="0,24,0,0" />

            <Slider Header="Vibration" 
                    Maximum="2" 
                    Minimum="0"
                    Margin="0,24,0,0"
                    Visibility="{x:Bind ViewModel.Muted, Mode=OneWay, Converter={StaticResource BooleanToOppositeVisibilityConverter}}"
                    Value="{x:Bind ViewModel.Vibration, Mode=TwoWay, Converter={StaticResource VibrationLevelToNumberConverter}}"
                    ThumbToolTipValueConverter="{StaticResource NumberToVibrationLevelStringConverter}" />
        </StackPanel>

        <CommandBar Grid.Row="2">
            <CommandBar.PrimaryCommands>
                <AppBarButton Label="Save"
                              Command="{x:Bind ViewModel.SaveCommand, Mode=OneWay}">
                    <AppBarButton.Icon>
                        <SymbolIcon Symbol="Save" />
                    </AppBarButton.Icon>
                </AppBarButton>
            </CommandBar.PrimaryCommands>
        </CommandBar>
    </Grid>
</Page>
